{
	"name": "template",
	"version": "1.0.0",
	"description": "template for pern projects",
	"author": "Kyle McKell <46072760+KyleMcKell@users.noreply.github.com>",
	"license": "MIT",
	"scripts": {
		"client-install": "yarn install --prefix client",
		"server-install": "yarn install --prefix server",
		"setup": "yarn server-install && yarn client-install"
	},
	"dependencies": {
		"concurrently": "^6.0.2"
	}
}
